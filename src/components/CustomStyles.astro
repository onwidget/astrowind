---
import '@fontsource/inter/variable.css';

// 'DM Sans'
// Nunito
// Dosis
// Outfit
// Roboto
// Literata
// 'IBM Plex Sans'
// Karla
// Poppins
// 'Fira Sans'
// 'Libre Franklin'
// Inconsolata
// Raleway
// Oswald
// 'Space Grotesk'
---

<style id="custom-styles" is:inline is:global>
  /*
  :root {
    --ph-font-sans: 'InterVariable';
    --ph-font-serif: var(--ph-font-sans);
    --ph-font-heading: var(--ph-font-sans);

    --ph-color-primary: rgb(30 64 175);
    --ph-color-secondary: rgb(30 58 138);
    --ph-color-accent: rgb(109 40 217);
    --ph-color-base: rgb(17 24 39);
    --ph-color-muted: rgb(75 85 99);
    --ph-color-bg-page: rgb(255 255 255);

    --ph-color-info: rgb(58 191 248);
    --ph-color-success: rgb(54 211 153);
    --ph-color-warning: rgb(251 189 35);
    --ph-color-error: rgb(248 114 114);

    --ph-radii-image: 6px;
    --ph-radii-btn: 9999px;

    --ph-bg-btn: linear-gradient(99deg, var(--ph-color-primary) 0%, var(--ph-color-accent) 100%);
    --ph-bg-btn-hover: linear-gradient(99deg, var(--ph-color-accent) 0%,  var(--ph-color-primary) 100%);
  }
   */
</style>
